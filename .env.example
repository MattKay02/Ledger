# Supabase
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ExchangeRate-API (free tier — https://www.exchangerate-api.com)
VITE_EXCHANGE_RATE_API_KEY=your-api-key-here

# Anthropic — used server-side only in api/ledger-bot.js (never exposed to client)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Supabase service role key — used server-side only for JWT verification + rate limit writes
# Found in Supabase dashboard → Project Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=eyJ-your-service-role-key-here
